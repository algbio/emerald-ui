import React from 'react';

const SequenceDisplayPanel: React.FC = () => {
  // This panel is now a placeholder. Sequence lists are rendered in FastaFileUploader and UniProtSearch components directly.
  // If you want to show a summary or instructions, you can do so here. Otherwise, this can be removed.
  return null;
};

export default SequenceDisplayPanel;
